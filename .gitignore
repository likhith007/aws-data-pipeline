# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate.*.backup
*.tfplan
override.tf
override.tf.json

# Lambda test output
response.json

# Lambda build output
lambda/**/dist/
lambda/**/*.zip

# Credentials — never commit these
*.pem
*.env
.env*
credentials
